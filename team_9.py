import random

team_name = 'El Chapo 2'
strategy_name = 'El Chapo 2.0' 
strategy_description = 'b if they betray more often'

def move(my_history, their_history, my_score, their_score):
    ### my_history, their_history are strings.
    ### my_score, their_score are ints.
    if b >= c in their_history:
        return b
    elif b == 0 and c == 0 in their_history:
        random.randint(1,2)
        if random.randint == 1:
            return b
        else:
            return c      
    else:
        return c

